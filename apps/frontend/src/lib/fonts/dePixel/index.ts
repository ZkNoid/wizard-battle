import localFont from 'next/font/local';

export const dePixel = localFont({
  src: './DePixelHalbfett.ttf',
  variable: '--font-pixel',
});

export const dePixelBreit = localFont({
  src: './DePixelBreit.ttf',
  variable: '--font-pixel-breit',
});

export const dePixelKlein = localFont({
  src: './DePixelKlein.ttf',
  variable: '--font-pixel-klein',
});

export const dePixelSchmal = localFont({
  src: './DePixelSchmal.ttf',
  variable: '--font-pixel-schmal',
});

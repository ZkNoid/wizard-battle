# UI Sound Effects

## 📍 Куда добавить файлы

Поместите UI звуки в эту папку:

```
apps/frontend/public/audio/sfx/ui/
├── modal-open.mp3       ✅ Добавлено (Этап 3)
├── modal-close.mp3      ✅ Добавлено (Этап 3)
├── hover.mp3            ✅ Добавлено (Этап 4)
└── click.mp3            ❌ НУЖНО ДОБАВИТЬ (Этап 5)
```

## 🎵 Рекомендуемые звуки (из вашего списка)

### Открытие вкладок/модалей
- **UI Medieval Menu Light Open**
  - Ссылка: https://www.pond5.com/ru/sound-effects/item/152697101-ui-medieval-menu-light-open-01
  - Файл: `modal-open.mp3`
  - Статус: ✅ Добавлено

### Закрытие вкладок/модалей
- **UI Medieval Menu Light Close**
  - Ссылка: https://www.pond5.com/ru/sound-effects/item/152697100-ui-medieval-menu-light-close-01
  - Файл: `modal-close.mp3`
  - Статус: ✅ Добавлено

### Наведение на кнопки (Этап 4)
- **UI Hover/Rollover/Select**
  - Ссылка: https://www.pond5.com/ru/sound-effects/item/192536642-ui-hover-rollover-select-10
  - Файл: `hover.mp3`
  - Статус: ✅ Добавлено

### Клик по кнопкам (Этап 5)
- **UI Click**
  - Рекомендация: Короткий щелчок (150-250ms)
  - Файл: `click.mp3`
  - Статус: ❌ НУЖНО ДОБАВИТЬ

## ⚙️ Формат файлов

- **Обязательно MP3!**
- Рекомендуемое качество: 128kbps - 192kbps (звуки короткие, не нужно высокое качество)
- Длительность: 0.2-0.5 секунды

## 🔄 Конвертация

Если у вас WAV, конвертируйте в MP3:

```bash
# С помощью FFmpeg
ffmpeg -i modal-open.wav -codec:a libmp3lame -q:a 4 modal-open.mp3
ffmpeg -i modal-close.wav -codec:a libmp3lame -q:a 4 modal-close.mp3
```

Параметр `-q:a 4` = хорошее качество для коротких звуков.

## ✅ Как это работает

### Автоматическое воспроизведение

Звуки играют автоматически благодаря хуку `useModalSound()`:

```typescript
export default function MyModal({ onClose }) {
  useModalSound(); // 🎵 Автоматически играет звуки!
  
  return <div>...</div>;
}
```

- **При открытии модали** → играет `modal-open.mp3`
- **При закрытии модали** → играет `modal-close.mp3`

### Интегрировано в модалях:

- ✅ InventoryModal (инвентарь)
- ✅ CraftModal (крафт)
- ✅ ExpeditionModal (экспедиции)
- ✅ HowToPlay (руководство)
- ✅ Support (поддержка)

### Hover звуки (Этап 4)

Hover звуки работают через prop `enableHoverSound`:

```typescript
<BoxButton 
  onClick={handleClick}
  enableHoverSound  // 🎵 Включаем hover звук
>
  Наведи на меня!
</BoxButton>

<Button 
  variant="blue"
  onClick={handleClick}
  enableHoverSound  // 🎵 Тоже работает с Button
>
  И на меня!
</Button>
```

**Интегрировано в:**
- ✅ Кнопки главного меню (Market, Inventory, Mail, Tournaments)
- ✅ Кнопки настроек (Support ?, Settings ⚙️, Audio 🔊)
- ✅ Все поддерживают prop `enableHoverSound`

### Click звуки (Этап 5)

Click звуки работают через prop `enableClickSound`:

```typescript
<BoxButton 
  onClick={handleClick}
  enableClickSound      // 🔊 Включаем click звук
  enableHoverSound      // 🎵 Можно комбинировать с hover
>
  Кликни на меня!
</BoxButton>

<Button 
  variant="blue"
  onClick={handleClick}
  enableClickSound      // 🔊 Тоже работает с Button
  enableHoverSound      // 🎵 Полная аудио-интеграция!
>
  И на меня тоже!
</Button>
```

**Интегрировано в:**
- ✅ Кнопки главного меню (Market, Inventory, Mail, Tournaments)
- ✅ Кнопки настроек (Support ?, Settings ⚙️, Audio 🔊)
- ✅ Все кнопки поддерживают как hover, так и click звуки

## 🧪 Тестирование

После добавления файлов:

1. Перезапустите dev server: `pnpm dev`
2. Откройте http://localhost:3000
3. Кликните на иконку инвентаря (сверху по центру)
   - Должен играть звук открытия
4. Закройте инвентарь (крестик)
   - Должен играть звук закрытия
5. Повторите для других модалей:
   - Craft (левый домик)
   - Expeditions (нижний левый угол)

## 🎨 Добавить свои звуки?

Обновите `src/lib/constants/audioAssets.ts`:

```typescript
ui: {
  modalOpen: '/audio/sfx/ui/modal-open.mp3',
  modalClose: '/audio/sfx/ui/modal-close.mp3',
  customSound: '/audio/sfx/ui/my-custom-sound.mp3',  // Новый звук
}
```

Затем используйте:

```typescript
const playSound = useSound();
playSound('customSound');
```

## 💡 Советы

### Громкость звуков

Если звуки слишком громкие/тихие:
1. Используйте AudioSelector (справа вверху)
2. Или отредактируйте файл в аудио редакторе (Audacity):
   - Эффект → Amplify/Normalize
   - Уменьшите на 6-12 dB для комфортной громкости

### Длительность

Рекомендуемая длительность UI звуков:
- **Открытие**: 200-400ms (быстро, энергично)
- **Закрытие**: 150-300ms (короче чем открытие)

Слишком долгие звуки будут раздражать при частом использовании.

## 📋 Следующие этапы

После добавления звуков открытия/закрытия:

- **Этап 4**: Hover эффекты (наведение на кнопки)
- **Этап 5**: Click эффекты (клики по кнопкам)
- **Этап 6**: Игровые звуки (заклинания, стрелы)

Все эти звуки тоже будут лежать в этой папке! 🎮
